aliases:
  grep-all:
    command: '!f() { git rev-list --all | xargs git grep "$@"; }; f'
    description: Find text in any commit ever
    category: grep
    risk: medium

  grep-group:
    command: grep --break --heading --line-number --color
    description: Find text and group the output lines
    category: grep
    risk: safe
