aliases:
  remotes-prune:
    command: '!git remote | while read -r remote; do git remote prune "$remote"; done'
    description: Prune all stale references for every remote
    category: remote
    risk: medium
