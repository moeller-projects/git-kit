aliases:
  a:
    command: add
    description: Shortcut for git add
    category: shortcuts
    risk: medium

  b:
    command: branch
    description: Shortcut for git branch
    category: shortcuts
    risk: safe

  c:
    command: commit
    description: Shortcut for git commit
    category: shortcuts
    risk: medium

  d:
    command: diff
    description: Shortcut for git diff
    category: shortcuts
    risk: safe

  f:
    command: fetch
    description: Shortcut for git fetch
    category: shortcuts
    risk: safe

  g:
    command: grep
    description: Shortcut for git grep
    category: shortcuts
    risk: safe

  l:
    command: log
    description: Shortcut for git log
    category: shortcuts
    risk: safe

  m:
    command: merge
    description: Shortcut for git merge
    category: shortcuts
    risk: medium

  o:
    command: checkout
    description: Shortcut for git checkout
    category: shortcuts
    risk: medium

  p:
    command: pull
    description: Shortcut for git pull
    category: shortcuts
    risk: medium

  s:
    command: status
    description: Shortcut for git status
    category: shortcuts
    risk: safe

  w:
    command: log --diff-filter=D --summary
    description: Show commits with deleted files (replaces deprecated git whatchanged)
    category: shortcuts
    risk: safe

  sw:
    command: switch
    description: Shortcut for git switch (modern checkout alternative, requires git >= 2.23)
    category: shortcuts
    risk: medium
    since_version: "2.23"
